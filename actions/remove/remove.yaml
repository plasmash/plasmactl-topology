runtime: plugin
action:
  title: Remove
  description: Remove a zone path
  arguments:
    - name: zone
      title: Zone
      description: Zone path to remove
      required: true
  options:
    - name: dir
      shorthand: d
      title: Directory
      description: Working directory (defaults to current)
      type: string
      default: "."
    - name: dry-run
      title: Dry Run
      description: Show what would be checked without removing
      type: boolean
      default: false
  result:
    type: object
    properties:
      zone:
        type: string
        description: The zone path that was removed
      dry_run:
        type: boolean
        description: Whether this was a dry run
      allocated_nodes:
        type: array
        description: Nodes allocated to this zone path
        items:
          type: string
      attached_components:
        type: array
        description: Components attached to this zone path
        items:
          type: string
